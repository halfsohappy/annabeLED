/*

rotary encoder selects hsv values (hey! backlead doesn't even need to know the patterns! it can just send a number! you're so smart annie!)

use left/right to select current setting the encoder is modifying (cast to A h on row 1, s on row 2, v on row 3)

have imaginary rows 4 and 5 that when scrolled throguh will let you switch to state mode? or hell, just put states in figure list.

use up and down to select a figure

use center button to send a figure

*/

